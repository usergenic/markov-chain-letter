import {MclApp} from '../../browser/elements/mcl-app';
import {MclSourcePanel} from '../../browser/elements/mcl-source-panel';

describe('Elements', () => {
  describe('MclApp', () => {
    it('is an element');
  });

  describe('MclSourcePanel', () => {
    it('is an element');
  });
});
